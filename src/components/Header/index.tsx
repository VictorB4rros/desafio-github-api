import './styles.css';

export default function Header() {

    return (
        <header className="header">
            <nav>
                <p className="header-title">Github API</p>
            </nav>
        </header>
    );
}